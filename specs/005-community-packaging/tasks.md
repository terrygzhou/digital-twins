# Tasks: Community Packaging & Documentation

**Feature**: `specs/005-community-packaging` · **Constitution**: Test-First (III) — every task that writes code/tests has its RED test first and fails before it passes. Pure docs/config/config-file tasks may skip the red step but MUST note "no red step (docs/config)". Host-neutrality (I, CRITICAL) and config-first (IV) apply to every deliverable.

**Baseline**: 005 is a **docs/packaging** slice. It ships **no new config knob**, **no new table**, **no new migration**, and no new ingestion behavior. It packages, licenses, documents, and version-policies the surface that 001–004 already built. Two standing guards are **extended** (the only test code 005 writes): `tests/unit/test_knob_docs.py` (gains a 100% knob-coverage class) and `tests/integration/test_portability.py` (gains the new 005 artifacts to its `SHIPPED` list). The version is single-sourced in `digital_twins/__init__.py` (hatchling reads it; `pyproject.toml` `dynamic=["version"]` is left untouched per R15). 005 ships as **0.5.0** (minor over 0.4.0, which is 004's MCP slice): new docs/packaging, no config-breaking change, no schema change.

> **Locked decisions honored (do not re-open):** R1/c1 (v1 user-builds the image; no pushed image; bundle = shipped reference deployment) · R2/c2 (config reference in `docs/configuration.md`; README links, stays 5-minute-sized) · R3/c3 (in-repo `.github/ISSUE_TEMPLATE` bug+feature+config-breaking-change + `docs/semver-policy.md`; NO live GitHub) · R4/c4 (SC-005 automated = compose schema/consistency + host-neutrality guard: 5 services, pinned, no `:latest`, no host paths; manual = `docker compose up`) · R5/c5 (SC-006 automated = valid wheel build + `pip show` reports MIT + runbook exists; manual = TestPyPI) · R6/c6 (host-neutrality CRITICAL; config reference = 100% knob coverage; deprecation warning REQUIRED ≥1 fixture) · R14 ("health" = `validate`, no standalone health command) · R15 (license form `{ text = "MIT" }` kept as-is; pyproject manifest NOT touched) · R16 (embedding-model = thin sentence-transformers HTTP service) · R17 (`python:3.11-slim` base image passes the portability guard; image-ref form ≠ host pin) · D-1 resolved (bundle SGLang + BGE-small-en-v1.5, pinned, external endpoints opt-in via env).

## Dependency Graph (delivery order)

```
Phase 1 (Config reference + its guard — the spine of 005)
   1.  RED  TestKnobsDocumentedInConfigurationDoc (docs/configuration.md)   <- nothing
   2.  GREEN docs/configuration.md (100% knobs, 6 GROUP_* tables)          <- (1)

Phase 2 (README quick start)
   3.  README.md 5-min quick start + docs index + Docker note             <- no red (docs)

Phase 3 (License + versioning)
   4.  confirm LICENSE (MIT) + pyproject license field (no change)         <- no red (docs/config)
   5.  docs/semver-policy.md                                               <- no red (docs)
   6.  version bump 0.4.0 -> 0.5.0 in digital_twins/__init__.py            <- no red (config, single-source)

Phase 4 (In-repo tracker)
   7.  .github/ISSUE_TEMPLATE/ (config + bug + feature + config-breaking)  <- no red (config)
   8.  docs/semver-policy.md cross-ref to the tracker                      <- no red (docs)

Phase 5 (Docker/Compose reference deployment)
   9.   RED  compose well-formedness + 5 services + pinned + host-neutral  <- nothing
   10.  GREEN docker-compose.yml (5 services, pinned, named vols, healthchecks) <- (9)
   11.  GREEN Dockerfile (python:3.11-slim, host-neutral)                  <- (9)

Phase 6 (PyPI + release runbook)
   12.  RED  wheel build + pip show MIT + runbook exists                   <- nothing
   13.  GREEN docs/release-runbook.md                                      <- (12)

Phase 7 (004 agent guides)
   14. docs/references/agent-guides.md (6 MCP tools + 4 stubs)             <- no red (docs)

Phase 8 (Deprecation warning mechanism)
   15.  RED  one-run deprecation warning fixture                            <- nothing
   16.  GREEN config-loader deprecation warning + test fixture             <- (15)

Phase 9 (Final guards + ledger)
   17. extend test_portability.py SHIPPED list (+ optional compose structural test)
   18. full suite green + CHANGELOG 0.5.0 entry + close spec gates
```

Critical path: **1 → 2 → (17)**: the red knob-coverage test, the config doc that satisfies it, then the portability guard that scans the whole 005 surface. Phases 2, 3, 4, 7 are docs/config and run in parallel once Phase 1's doc exists. Phases 5 and 6 are the two red-test/green-implementation pairs. Phase 8 adds the single deprecation fixture. Phase 9 is the gate.

## Phase 1: Config reference + its guard (the SC-003 spine)

- [x] **T001** `[US1][US4]` — **RED test first (constitution III).** Extend `tests/unit/test_knob_docs.py` with a new class `TestKnobsDocumentedInConfigurationDoc` (research R4, C-2) that parses `docs/configuration.md` and asserts three invariants: (1) **every** key in the `KNOBS` registry appears as a doc row (100% coverage — a new knob with no row fails); (2) **no phantom knob** — no doc row names a knob absent from `KNOBS`; (3) **in sync** — each row's documented `env` var and `default` match the registry entry. The parser is a lightweight Markdown-table reader (the doc uses a fixed `| knob | type | default | env | notes |` table-per-group format, so it is a line scan — same "parse the committed artifact" philosophy as the existing `config.example.yml` parser). Add a `module`-scoped fixture that reads `docs/configuration.md` (guard against a missing file: the test **fails** now because the doc does not exist yet — this is the RED state). The existing 001 classes (`TestKnobRegistryStructure`, `TestKnobsDocumentedInConfigExample`, `TestKnobsDocumentedInEnvExample`, `TestConfigExampleKnobsInRegistry`, `TestEnvExampleVarsInRegistry`, `TestEnvVarConsistency`, `TestSchedulerStatusPort`) are left intact and must stay green. Run `pytest tests/unit/test_knob_docs.py` and confirm the new class **fails** (doc missing) while the old classes pass.
- [x] **T002** `[US1][US4]` — **GREEN** — write `docs/configuration.md` so the T001 class passes: the single authoritative human-readable config reference (C-2). Structure mirrors the six `GROUP_*` sections in `digital_twins/config/knobs.py`: **Global, Endpoints, Embedding, Chunking, Scheduler, Sources** — each a table of `| knob | type | default | env var | notes |`. Cover 100% of `KNOBS`: the global knobs (`state_dir`, `config_dir`), the endpoint knobs (`qdrant.url`, `qdrant.api_key`, `neo4j.url`, `neo4j.user`, `neo4j.password`, `llm.endpoint`, `llm.model`, `llm.api_key`), the embedding knobs (`embedding.model` pinned `BAAI/bge-small-en-v1.5` 384-dim, `embedding.device` `auto|cpu|cuda`), the chunking knobs (`chunking.max_chars` 800, `chunking.overlap` 100), the scheduler knob (`scheduler.status_port` 8765, `0` disables), and every per-built-in-source knob (`sources.<name>.enabled|max_items|timeout_s` for each of `hermes, pi, dsh, paperclip, yahoo, gmail, fs`) plus the credential/account env knobs (`sources.yahoo.credential`=YMAIL_APP_PASSWORD, `sources.yahoo.email`=YMAIL_EMAIL, `sources.gmail.credential`=GMAIL_APP_PASSWORD, `sources.gmail.email`=GMAIL_EMAIL) and the custom-source example (`sources.mytool.enabled|entrypoint|credential|prefix`). Include a **precedence** section (verbatim: `env (incl. .env) → kb.local.yml → kb.yml → user_config (per-user, 003) → built-in defaults`; env wins; `KB_` prefix, `__` = nesting) and a **"how env vars map"** section (`KB_` prefix, `__` = nesting, with examples). Also document the **deprecation mechanism** (C-2 edge case): a one-run warning naming the replacement (the fixture itself lives in `tests/`, not here — the doc describes the behavior). Host-neutral (no host path / `python3.N` pin / username / `~/.` absolute). After writing, `pytest tests/unit/test_knob_docs.py` is fully green. **T001 blocks T002.**
- [x] **T003** `[US1][US4]` — Manual spot-check of the doc against quickstart Scenario A (C-2 / quickstart): open `docs/configuration.md`, confirm `scheduler.status_port` (default `8765`, env `KB_SCHEDULER__STATUS_PORT`), `embedding.model` (default `BAAI/bge-small-en-v1.5`, 384-dim), and `sources.fs.enabled` (default `false`, env `KB_SOURCES__FS__ENABLED`) are each present with type, default, and env mapping. No code change; record the result in the SDD ledger. (No red step — verification task.)

## Phase 2: README quick start

- [x] **T004** `[US1]` — **no red step (docs/config).** Rewrite `README.md` to the C-1 shape, in order: (1) **What it is** — one paragraph (portable KB ingestion: layered config, fail-fast named sources, deterministic dedup-safe ingest into user-supplied Qdrant/Neo4j); (2) **5-minute quick start** — the exact host-neutral `install → init → validate → first run` sequence from research R5 / quickstart (the `KB_STATE_DIR` placeholder, `pip install .` / `pip install digital-twins`, `digital-twins init`, `digital-twins validate` labeled "validate (health check)" per R14, and the no-credential `fs` demo source first run with the `fs: N item(s)` / second-run `fs: 0 item(s)` dedup note); (3) **Configuration** — a pointer to `docs/configuration.md` + a one-line precedence note (NOT the full knob table — c2 keeps the README 5-minute-sized); (4) **Add a new source** — the custom-source how-to (the `mytool` example: `entrypoint`/`credential`/`prefix`, register in `kb.local.yml`, `run --source mytool`); (5) **Docs index** — links to `docs/configuration.md`, `docs/scheduling.md` (002), `docs/multi-user.md` (003), `docs/references/agent-guides.md` (004), `docs/release-runbook.md`, `docs/semver-policy.md`; (6) **Docker** — the two-mode note (bundled-only / bundled-with-external-endpoints) + `docker compose up` (manual, needs a Docker host). Host-neutral throughout (passes `test_portability.py`). (No red step — pure docs; host-neutrality is verified by the Phase 9 guard.)

## Phase 3: License + versioning

- [x] **T005** `[US2]` — **no red step (docs/config, verify-only).** Confirm the license surface (C-3 / US2): `LICENSE` at the repo root carries the MIT text (copyright holder = project owner); `pyproject.toml` declares `license = { text = "MIT" }` **and is NOT modified** (R15 — the legacy PEP 621 form already satisfies `pip show`; the SPDX-string cleanup is a documented follow-up, out of 005 scope). No file changes; record the confirmation in the SDD ledger. The automated proof that `pip show` reports MIT lands in Phase 6 (T012/T013).
- [x] **T006** `[US3]` — **no red step (docs).** Write `docs/semver-policy.md` (C-4 / BR-11.6.3) with the sections: (1) **SemVer baseline** — `major.minor.patch` single-sourced in `digital_twins/__init__.py` (hatchling reads it; `pyproject.toml` `dynamic=["version"]` is untouched); (2) **What each bump means** — the mapping table (new non-breaking knob/feature/docs = `minor`/`Added`; bug/typo/config-doc fix = `patch`; **config-breaking = `major` + migration note**; deprecation where the old knob still works = `minor`/`Deprecated`); (3) **Config-breaking definition** — the precise trigger set (removing/renaming a knob, changing a knob's declared type, changing default semantics, or changing the env-var mapping); (4) **Mapping to the tracker** — a config-breaking change is proposed via `.github/ISSUE_TEMPLATE/config-breaking-change.yml` (required `version_impact: major` + required `migration_note`), and at release the runbook cross-checks that the changelog's breaking entry forces the major bump; (5) **Changelog linkage** — every release since 0.1.0 has a `CHANGELOG.md` entry; a breaking release's notes carry the migration note. Host-neutral.
- [x] **T007** `[US3][US4]` — **no red step (config, single-source).** Bump the version `0.4.0 → 0.5.0` in `digital_twins/__init__.py` **only** (the single source; hatchling's `[tool.hatch.version] path` picks it up — do not touch `pyproject.toml`). Confirm `digital-twins --version` / `--version-json` report `0.5.0`. The `0.5.0` CHANGELOG entry is written in Phase 9 (T018).

## Phase 4: In-repo tracker

- [x] **T008** `[US3]` — **no red step (config).** Create `.github/ISSUE_TEMPLATE/` with four files (R3 / c3 / C-5, GitHub-flavored, no live remote): `config.yml` (`blank_issues_enabled: false` + `contact_links` off; lists the three forms — the machine-readable entry point / tracker index); `bug_report.yml` (structured bug form with a fixed key set: `area`, `component`, `severity`, `repro`); `feature_request.yml` (structured feature form: `area`, `component`, `type`, `motivation`); and `config-breaking-change.yml` (structured form with a **required** `version_impact` field whose controlled vocabulary is `major|minor|patch` and a **required** `migration_note` field — the machine-readable link to the major-bump rule). No template references a host path / username / interpreter pin. (No red step — config files; host-neutrality verified by the Phase 9 guard.)
- [x] **T009** `[US3]` — **no red step (docs).** Add the cross-reference inside `docs/semver-policy.md` (T006) to the in-repo tracker: name the three templates + the `config-breaking-change.yml` `version_impact`/`migration_note` fields, and state that a config-breaking issue is the only path that forces `major`. (No red step — docs; host-neutrality verified by the Phase 9 guard.)

## Phase 5: Docker/Compose reference deployment

- [x] **T010** `[US5]` — **RED test first (constitution III, R4/c4).** Write the automated SC-005 check: a test that loads `docker-compose.yml` (well-formed YAML — use a YAML parser; if PyYAML is not in the test env, assert it parses with the same parser the package already depends on) and asserts the invariants from C-6/SC-005: (a) **five required services present** — `qdrant`, `neo4j`, `digital-twins`, `llm`, `embedding-model`; (b) **every `image:` is pinned** — an explicit version tag on every service image, and **no `:latest`** and no untagged image; (c) **host-neutral** — no host path (`/home/...`, `/Users/...`, `~/...` absolute), no `python3.N` host-pin shorthand (a `python:3.11-slim` *image reference* is allowed per R17), no literal username; the `digital-twins` service uses **named volumes** (not host bind paths) for state and mounts its config read-only (config-in, image-out). Place this test under `tests/integration/` (e.g. extend `test_portability.py` or add `test_docker_compose.py`). The test **fails** now because `docker-compose.yml` does not exist yet (RED). Run it and confirm the RED failure.
- [x] **T011** `[US5]` — **GREEN** — write `docker-compose.yml` (repo root) so T010 passes (R1 / C-6): exactly five services, all pinned (placeholder tags from R1: `qdrant/qdrant:1.9.7`, `neo4j/neo4j:5.18-community`, `lmsys/sglang:v0.4.2`, `embedding-model` on `python:3.11-slim` with `BAAI/bge-small-en-v1.5` baked in, `digital-twins` built locally via `build: .` / `digital-twins:0.4.0`). Named volumes (`qdrant-data`, `neo4j-data`, a `digital-twins-state` for `KB_STATE_DIR`) — no host binds. Each backend has a `healthcheck` (`qdrant` `:6333/healthz`, `neo4j` `cypher-shell ... RETURN 1`, `llm` `:8000/v1/models`, `embedding-model` `:8080/v1/models`). `digital-twins` sets `depends_on` with `condition: service_healthy` on all four backends so `init`/`serve` start only after they report healthy. The `digital-twins` service exposes the env-override surface (R1) via `environment:` mapping compose-level env to the package's `KB_*` knobs — `QDRANT_HOST`→`KB_QDRANT__URL` (default `http://qdrant:6333`), `NEO4J_HOST`→`KB_NEO4J__URL` (`bolt://neo4j:7687`), `NEO4J_USER`/`NEO4J_PASSWORD`→`KB_NEO4J__USER`/`__PASSWORD`, `LLM_ENDPOINT` (alias `SGLANG_ENDPOINT`)→`KB_LLM__ENDPOINT` (`http://llm:8000/v1`), `EMBEDDING_ENDPOINT`→embed service base (`http://embedding-model:8080/v1`) — these are **compose-level env, not new config-layer knobs** (the `KNOBS` registry and `test_knob_docs.py` are unaffected). The `embedding-model` service (R16) is a thin `sentence-transformers` HTTP server exposing `POST /v1/embeddings` on the pinned `BAAI/bge-small-en-v1.5` so the host `digital-twins` container reaches it without an external embedding service. D-1: SGLang + BGE-small-en-v1.5 are bundled by default; external endpoints are an opt-in env override. After writing, the T010 test is green. **T010 blocks T011.**
- [x] **T012** `[US5]` — **GREEN** — write `Dockerfile` (repo root) so the `digital-twins` image builds (R1 / C-6 / R17): base `python:3.11-slim` (the container base image — the image-reference form, which the `INTERPRETER_PIN` pattern `python3\.\d+` does NOT match, so it passes the portability guard as written; matches the package's `requires-python >= 3.11`), install the package via the hatchling build (`pip install .` / the PEP 517 backend), copy the `digital_twins` package, and expose the `digital-twins` console script. Host-neutral: no host path / `python3.N` host-pin shorthand / literal username. If any line would trip the guard, use the image-reference form or add a narrowly-scoped allow-list entry to the guard with a comment — never a silent pattern edit (C-9 note). The manual proof is `docker compose up` on a Docker host (out of suite). **T010 blocks T012.**

## Phase 6: PyPI + release runbook

- [x] **T013** `[US6]` — **RED test first (constitution III, R5/c5).** Write the automated SC-006 check: a test that (a) builds a valid wheel in a temp venv via the hatchling/PEP 517 backend (`python -m build --wheel` → `dist/digital_twins-<v>-py3-none-any.whl`), (b) installs that wheel and asserts `pip show digital-twins` reports `License: MIT`, (c) asserts `LICENSE` exists at the repo root, and (d) asserts `docs/release-runbook.md` exists. Mark it appropriately for a temp-venv build (it is the in-suite automated proof; the live TestPyPI round-trip is manual and NOT in this test). The test **fails** now because `docs/release-runbook.md` does not exist yet (RED). Place it under `tests/integration/` (e.g. `test_pypi_build.py`). Run it and confirm the RED failure.
- [x] **T014** `[US6]` — **GREEN** — write `docs/release-runbook.md` (R2 / C-7) so T013 passes: the repeatable release procedure any maintainer follows cold. Sections: (1) **Pre-release** — bump `__version__` in `digital_twins/__init__.py` (single source; never edit the wheel name by hand), add the `CHANGELOG.md` entry (Keep a Changelog `Added`/`Changed`/`Deprecated`/`Migrated`), confirm `test_portability.py` + `test_knob_docs.py` green; (2) **Build** — `python -m build` in a clean venv → `dist/`; (3) **Automated in-suite check** — wheel builds + `pip show` reports MIT; (4) **TestPyPI (manual, credential-gated)** — `twine upload --repository-url https://test.pypi.org/legacy/ dist/*` with a TestPyPI token, then `pip install --index-url https://test.pypi.org/simple/ digital-twins` round-trip; (5) **PyPI (manual, credential-gated)** — `twine upload dist/*` with a PyPI API token; (6) **Tag** — `git tag v<version>`; (7) **Record the actual image tags** used in the Docker bundle (resolving R1's placeholder tags). Name the TestPyPI/PyPI steps explicitly **manual** (network + credentials); no host paths / credentials / `python3.N` pin. Note the SPDX `license = "MIT"` upgrade as a future polish item (R15). After writing, the T013 test is green. **T013 blocks T014.**

## Phase 7: 004 agent guides

- [x] **T015** `[US4]` — **no red step (docs).** Write `docs/references/agent-guides.md` (R6 / C-8, BR-11.5.2) — the canonical "any MCP agent" onboarding page, written against the **stable 004 contract** (`specs/004-mcp-scheduler-tools/contracts/scheduler.md`; 004's implementation may not have landed, so the doc is correct as long as 004 keeps its agent-facing field names): (1) **Onboarding line** — an agent with a valid token + transport URL gets the full tool set; the token is the only host-specific thing (NFR-13); (2) **The six 004 scheduler tools** — `kb_schedule_list`, `kb_schedule_create`, `kb_schedule_update`, `kb_schedule_delete`, `kb_schedule_run`, `kb_run_history`, each with name, args (mirroring 003/004 field names `schedule_id`, `source`, `preset`, `param`, `fire_time`, `acl`, plus the optional `agent_kind`), the shared success/error envelope, the **role requirement** (`kb_schedule_list`=query_status own; `kb_schedule_create/update/delete`=schedule_crud; `kb_schedule_run`=trigger_run; `kb_run_history`=view_own_history own / view_all_history all-admin; a `reader` may list + own-history but is denied every mutating tool with `code=permission_denied`), and the stable error codes (`unauthorized`, `permission_denied`, `schedule_not_found`, `invalid_preset`, `invalid_param`, `conflict`, `source_disabled`, `run_failed`, `internal_error`, `not_implemented_yet`); (3) **The four stubbed BR-10 tools** — `kb_search`, `kb_chat`, `kb_ingest`, `kb_health`, documented as registered-but-stubbed in v1 (each returns `code=not_implemented_yet` naming the follow-up slice); (4) **Transport** — stdio + HTTP/SSE, a community user points any MCP client at their local `kb-mcp` with a token. Host-neutral. (No red step — docs; host-neutrality verified by the Phase 9 guard. A 005 review-time cross-check confirms the tool names match 004's registry once 004 lands.)

## Phase 8: Deprecation warning mechanism

- [x] **T016** `[US4]` — **RED test first (constitution III, C-2 edge / US4-AS3).** Write a test with **≥1 fixture** proving the required deprecation mechanism: when a deprecated config value/knob is read, the user gets a **one-run** deprecation warning that **names the replacement** (005 documents the mechanism; it does not deprecate any real shipped knob — the fixture uses a synthetic deprecated key). The test asserts: the warning fires exactly **once** on the first read in a process (subsequent reads in the same process do not re-warn), and the warning message names the replacement knob/command. Place it under `tests/unit/` (e.g. `test_deprecation_warning.py`). The test **fails** now because the warning mechanism is not yet in the config layer (RED). Run it and confirm the RED failure.
- [x] **T017** `[US4]` — **GREEN** — implement the one-run deprecation warning in the config layer (where a deprecated knob/alias is resolved) so T016 passes: a single-emit, per-process warning (e.g. `warnings.warn` gated so it fires once, or a module-level "already warned" flag) whose message names the replacement. Keep it host-neutral and ensure it does not alter the documented `KNOBS` surface (the warning is a *mechanism*, not a new knob). After implementing, the T016 test is green. **T016 blocks T017.**

## Phase 9: Final guards + ledger

- [x] **T018** — **no red step (config/ledger).** Add the new 005 artifacts to `tests/integration/test_portability.py`'s `SHIPPED` list: `docs/configuration.md`, `docs/semver-policy.md`, `docs/release-runbook.md`, `docs/references/agent-guides.md`, `docker-compose.yml`, `Dockerfile`, and `.github/ISSUE_TEMPLATE/` (all files). Re-run the existing three patterns (`HOST_PATTERNS`, `INTERPRETER_PIN`, `LITERAL_USERNAMES`) over them — **no new pattern** is needed (C-9: the existing patterns already cover 005's artifacts; the `python:3.11-slim` image-reference form in the Dockerfile/compose is the known boundary that passes). Confirm `pytest tests/integration/test_portability.py` is green.
- [x] **T019** — Full-suite green + ledger. Run the entire `pytest` suite and confirm: `tests/unit/test_knob_docs.py` green (100% knob coverage of `docs/configuration.md` — SC-003), `tests/integration/test_portability.py` green (all 005 artifacts host-neutral — SC-006/NFR-13), the T010 compose test green (SC-005 automated: 5 services, pinned, no `:latest`, no host paths), the T013 PyPI build test green (SC-006 automated: valid wheel + `pip show` MIT + runbook exists), and the T016/T017 deprecation fixture green (US4/FR-4 REQUIRED). Add the `0.5.0` `CHANGELOG.md` entry (Added: config reference, README quick start, semver policy, in-repo tracker, Docker/Compose bundle, release runbook, agent guides, deprecation mechanism; no Changed/Deprecated/Migrated — 005 adds no config-breaking change). Record all results in the SDD ledger (`.superpowers/sdd/`). (No red step — verification/ledger task.)

## Notes

- **Parallel sets**: T004 (README) ∥ T006 (semver-policy) ∥ T008 (ISSUE_TEMPLATE) ∥ T015 (agent-guides) — all docs/config, independent once T002's config doc exists. T011 ∥ T012 (compose ∥ Dockerfile) both satisfy the single T010 red test.
- **No host paths anywhere** (CRITICAL, constitution I): every 005 artifact (README, docs/*, docker-compose.yml, Dockerfile, .github/*, and the version-bumped `__init__.py`) passes `test_portability.py`. The compose env-override surface (`QDRANT_HOST`, `LLM_ENDPOINT`, …) is compose-level deployment env, not a config-layer knob, so it never enters `KNOBS` and never breaks `test_knob_docs.py`.
- **Single-source version** (005 invariants): only `digital_twins/__init__.py` is edited for the `0.4.0 → 0.5.0` bump; `pyproject.toml` `dynamic=["version"]` + the `license = { text = "MIT" }` form are left untouched (R15). The deferred SPDX-string cleanup is a documented follow-up, not a 005 change.
- **Test-first summary (constitution III)**: red-then-green pairs are T001→T002 (config doc), T010→T011/T012 (compose + Dockerfile), T013→T014 (runbook + wheel build), T016→T017 (deprecation warning). All pure docs/config tasks (T004, T005, T006, T007, T008, T009, T015) note "no red step (docs/config)" and are verified by the Phase 9 portability/knob guards instead.
- **Manual (out-of-suite) proofs** — documented, not automated: `docker compose up` → healthy stack → in-container `digital-twins validate` exits 0 (SC-005 manual, needs a Docker host); TestPyPI/PyPI round-trip (SC-006 manual, needs network + credentials); the clean-machine 5-minute README run (SC-001).
- **Task IDs are feature-scoped** (this file), matching 001/002/003's ledger convention; the SDD ledger (`.superpowers/sdd/`) tracks execution.
