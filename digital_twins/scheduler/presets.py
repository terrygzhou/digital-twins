"""Preset cadences: expand_next pure function (002 contracts/scheduler.md).

Pure functions, no I/O. Property-testable (SC-003): the same
(preset, param, fire_time, now, anchor) always yields the same fire time.
"""
